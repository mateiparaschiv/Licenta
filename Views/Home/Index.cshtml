@using Microsoft.Extensions.Configuration
@inject IConfiguration _configuration
@model List<AlbumModel>
@{
    ViewData["Title"] = "Home Page";
    var environment = _configuration.GetValue<string>("Environment");
    var albumList = Model;
    var rand = new Random();
    var randomIndex = rand.Next(albumList.Count());

}
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<h2 class="text-muted">Home</h2>
<div class="text-center">
    @*<h1 class="display-2">Home</h1>*@
    <h1 class="display-4">In @environment</h1>
    <div>
        <div class="d-flex bd-example justify-content-center">
            @*style="max-width:450px; min-height:450px; min-"*@
            <div id="carouselExampleCaptions" class="carousel slide align-content-center" data-bs-ride="carousel">
                <div class="carousel-indicators">
                    <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-label="Slide 1" aria-current="true"></button>
                    <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2" class=""></button>
                    <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3" class=""></button>
                </div>
                <div class="carousel-inner ">
                    <div class="carousel-item active">
                        <a class="nav-link active p-0" asp-controller="Albums" asp-action="Album" asp-route-name="@albumList[randomIndex].Name">
                            <img src="~/images/Albums/@(albumList[randomIndex].Name).jpg" id="home_image">
                        </a>
                        <div class="carousel-caption d-none d-md-block">
                            <p id="index_text" class="h2 text-center">@albumList[randomIndex].Name</p>
                            <p id="index_text" class="h3 text-center">@albumList[randomIndex].Artist</p>
                        </div>
                    </div>
                    @for (int i = 0; i < 2; i++)
                    {
                        var index = rand.Next(albumList.Count());
                        <div class="carousel-item">
                            @*<img src="@albumList[index].Image" id="home_image">*@
                            <a class="nav-link active p-0" asp-controller="Albums" asp-action="Album" asp-route-name="@albumList[index].Name">
                                @*<img src="@album.Image" id="album_col">*@
                                @*<img src="@albumList[randomIndex].Image" id="home_image">*@
                                @*<img src="@albumList[index].Image" id="home_image">*@
                                <img src="~/images/Albums/@(albumList[index].Name).jpg" id="home_image">
                            </a>
                            <div class="carousel-caption d-none d-md-block">
                                <p id="index_text" class="h2 text-center">@albumList[index].Name</p>
                                <p id="index_text" class="h3 text-center">@albumList[index].Artist</p>
                            </div>
                        </div>
                    }
                    @*<div class="carousel-item">
                    <img src="@albumList[rand.Next(albumList.Count())].Image" id="home_image">
                    <div class="carousel-caption d-none d-md-block">
                    <h5>Second slide label</h5>
                    <p>Some representative placeholder content for the second slide.</p>
                    </div>
                    </div>
                    <div class="carousel-item">
                    <img src="@albumList[rand.Next(albumList.Count())].Image" id="home_image">
                    <div class="carousel-caption d-none d-md-block">
                    <h5>Second slide label</h5>
                    <p>Some representative placeholder content for the second slide.</p>
                    </div>
                    </div>*@
                </div>
                <button id="carousel_control" class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button id="carousel_control" class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
    </div>
</div>
