@using Microsoft.Extensions.Configuration
@inject IConfiguration _configuration
@model AlbumModel
@{
    ViewData["Title"] = "Index Albums";
    var environment = _configuration.GetValue<string>("Environment");
}
<div class="row py-4">
    <div class="col-md-4">
        @*<svg class="bd-placeholder-img" width="100%" height="250" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: Image" preserveAspectRatio="xMidYMid slice" focusable="false">
        <title>
        Placeholder
        </title>
        <rect width="100%" height="100%" fill="#868e96"></rect>
        <text x="50%" y="50%" fill="#dee2e6" dy=".3em">Image</text>
        </svg>*@
        @if (environment == "Development")
        {
            @*<img src="~/images/testImage.png" height="320" width="320" />*@
            <img src="//e.snmc.io/i/300/w/c8a40548b0fee5fa10095de3b97d3f90/10745116" alt="Pink Floyd - The Dark Side of the Moon" height="auto" width="auto">
            @*<img src="https://e.snmc.io/i/600/w/97747c35e070433549c1c691b4d97b28/7024368/death-grips-exmilitary-Cover-Art.jpg" height="350" width="100%" />*@

        }
        @if (environment == "Production")
        {
            <img src="~/images/musicNote.svg" height="320" width="320" />
        }
        <ul class="list-group list-group-flush list-group-numbered">
            @*
            justify-content-center text-right
            *@
            @foreach (var song in @Model.Songs.ToList())
            {
                <li class="list-group-item ">
                    <span>@song</span>
                </li>
            }
        </ul>
    </div>

    <div class="col-md-8">
        <div class="card-body">
            <a class="nav-link active p-0" asp-controller="Album" asp-action="Index" asp-route-name="@Model.Name">
                <h1 class="display-4">@Model.Name</h1>
            </a>
            <a class="nav-link active p-0" asp-controller="Artist" asp-action="Index" asp-route-name="@Model.Artist">
                <h1 class="display-6">@Model.Artist</h1>
            </a>
            <div class="d-flex flex-wrap justify-content-between align-items-center py-3  border-top">
                @*<span class="text-begin mb-3 mb-md-0">@artist.Genre</span>
                <span class="text-center mb-3 mb-md-0">@artist.Year</span>
                <span class="text-end mb-3 mb-md-0">@artist.Rating</span>*@

                <div class="container g-0">
                    <div class="row row-cols-3">
                        <span class="col text-begin mb-3 mb-md-0">Genre</span>
                        <span class="col text-center mb-3 mb-md-0">Year</span>
                        <span class="col text-end mb-3 mb-md-0">Rating</span>
                        <span class="col text-begin mb-3 mb-md-0">@Model.Genre</span>
                        <span class="col text-center mb-3 mb-md-0">@Model.Year</span>
                        <span class="col text-end mb-3 mb-md-0">@Model.Rating</span>
                    </div>
                </div>
            </div>
            <p class="card-text indent">@Model.Description</p>
        </div>
    </div>
</div>