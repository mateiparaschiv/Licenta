@using Microsoft.Extensions.Configuration
@inject IConfiguration _configuration
@model List<AlbumModel>
@{
    ViewData["Title"] = "Index Albums";
    var environment = _configuration.GetValue<string>("Environment");
}

@*<h1>Index Albums</h1>*@
@foreach (var artist in Model)
{
    <div class="row py-4">
        <div class="col-md-4">
            <img src="@artist.Image">
            @*<ul class="list-group list-group-flush list-group-numbered">
                @*
            justify-content-center text-right
            *@
            @*
                @foreach (var song in @artist.Songs.ToList())
                {
                    <li class="list-group-item ">
                        <span>@song</span>
                    </li>
                }
            </ul>
            *@
        </div>
        <div class="col-md-8">
            <div class="card-body">
                <a class="nav-link active p-0" asp-controller="Album" asp-action="Index" asp-route-name="@artist.Name">
                    <h1 class="display-4 text-center">@artist.Name</h1>
                </a>
                <a class="nav-link active p-0" asp-controller="Artist" asp-action="Index" asp-route-name="@artist.Artist">
                    <h1 class="display-6 text-center">@artist.Artist</h1>
                </a>
                @*
                <div class="d-flex flex-wrap justify-content-between align-items-center py-3 border-top">

                    @*
                <span class="text-begin mb-3 mb-md-0">@artist.Genre</span>
                <span class="text-center mb-3 mb-md-0">@artist.Year</span>
                <span class="text-end mb-3 mb-md-0">@artist.Rating</span>
                    *@
                @*
                    <div class="container g-0">
                        <div class="row row-cols-3">
                            <span class="col text-begin mb-3 mb-md-0">Genre</span>
                            <span class="col text-center mb-3 mb-md-0">Year</span>
                            <span class="col text-end mb-3 mb-md-0">Rating</span>
                            <span class="col text-begin mb-3 mb-md-0">@artist.Genre</span>
                            <span class="col text-center mb-3 mb-md-0">@artist.Year</span>
                            <span class="col text-end mb-3 mb-md-0">@artist.Rating</span>
                        </div>
                    </div>
                </div>
                *@
                @*<p class="card-text indent">@artist.Description</p>*@
            </div>
        </div>
    </div>
}